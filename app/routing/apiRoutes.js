// Create a GET route with urel /api/friends (this will be used to dsoplay a JSON of possible friends)
// Create a POST route to same database to hande incoming survey results. (also include the compatability logic)